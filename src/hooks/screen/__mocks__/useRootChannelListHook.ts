const useRootChannelListHook = {
  anonymousChannelUnreadCount: 10,
  signedChannelUnreadCount: 20,
  totalUnreadCount: 30,
  refreshAnonymousChannelList: jest.fn(),
  refreshSignedChannelList: jest.fn()
};

export default useRootChannelListHook;
